@using System.Diagnostics.CodeAnalysis
@model MassagerApp.PL.ViewModels.CreateChatViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div>
    <h1>Start chat/group with:</h1>
    <form method="post">
        <div>
            <label class="form-label" asp-for="UserIds"></label>
            <select class="form-select" required asp-for="UserIds" asp-items="ViewBag.Users">
            </select>
        </div>

        <div>
            <input type="submit" class="btn btn-primary" value="Add" />
        </div>
    </form>
</div>
